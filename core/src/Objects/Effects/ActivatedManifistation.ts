import Level from '../../Level'
import Effect from './Effects'

export default class ActivatedManifistation extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'activated manifistation'
    }
}
