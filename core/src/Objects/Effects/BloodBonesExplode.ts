import Level from '../../Level'
import Effect from './Effects'

export default class BloodBonesExplode extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'blood bones explode'
    }
}
