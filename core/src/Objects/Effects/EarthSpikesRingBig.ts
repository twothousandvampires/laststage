import Level from '../../Level'
import Effect from './Effects'

export default class EarthSpikesRingBig extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'earth spikes big'
    }
}