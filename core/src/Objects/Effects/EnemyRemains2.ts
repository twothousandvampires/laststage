import Level from '../../Level'
import Effect from './Effects'

export default class EnemyRemains2 extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'enemy remains2'
    }
}
