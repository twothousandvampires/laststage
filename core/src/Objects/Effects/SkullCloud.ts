import Level from '../../Level'
import Effect from './Effects'

export default class SkullCloud extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'skull cloud'
    }
}
