import Level from '../../Level'
import Effect from './Effects'

export default class SmallShockNova extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'small shocknova'
    }
}
