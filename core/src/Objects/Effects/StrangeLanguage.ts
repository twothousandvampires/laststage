import Level from '../../Level'
import Effect from './Effects'

export default class StrangeLanguage extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'strange language'
    }
}
