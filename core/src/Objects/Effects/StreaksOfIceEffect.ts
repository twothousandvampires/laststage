import Level from '../../Level'
import Effect from './Effects'

export default class StreaksOfIceEffect extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'streaks of ice'
    }
}