import Level from '../../Level'
import Effect from './Effects'

export default class TextLanguage2 extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'text l2'
    }
}
