import Level from '../../Level'
import Effect from './Effects'

export default class TextLanguage3 extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'text l3'
    }
}
