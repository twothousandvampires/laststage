import Level from '../../Level'
import Effect from './Effects'

export default class UltimatumArena extends Effect {
    constructor(level: Level) {
        super(level)
        this.name = 'ultimatum arena'
    }
}
